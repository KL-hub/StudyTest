查找的常用四种算法
1、线性查找
2、二分查找（前提是有序数组）
3、插值查找(二分查找的改进版，中间值为，减少递归的次数)
   int mid=left+(right-left)*(findVal-arr[left])/(arr[right]-arr[left])
   对于数据量大，关键字分布比较均匀的查找表来说，采用插值查找，速度较快。
   关键字不均匀的情况下，该方法不一定比折半查找要好。
4、斐波那契查找（有序数组）

